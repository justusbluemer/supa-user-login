{
	"name": "owntag-internal-utils",
	"version": "1.0.0",
	"description": "Some utility code that might come in handy for miscellaneous usecases",
	"main": "app.js",
	"scripts": {
		"build": "npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch",
		"start": "http-server -p 8080",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@tailwindcss/forms": "^0.5.7",
		"http-server": "^14.1.1"
	},
	"devDependencies": {
		"tailwindcss": "^3.4.1"
	}
}
