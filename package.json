{
	"name": "owntag-internal-utils",
	"version": "1.0.0",
	"description": "Some utility code that might come in handy for miscellaneous usecases",
	"main": "app.js",
	"scripts": {
		"build": "npx tailwindcss -i ./src/style.css -o ./public/style.css && npx copyfiles -u 1 ./src/app.js ./src/index.html ./public",
		"dev": "http-server ./public -p 8080",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"http-server": "^14.1.1"
	},
	"devDependencies": {
		"@tailwindcss/forms": "^0.5.7",
		"copyfiles": "^2.4.1",
		"tailwindcss": "^3.4.1"
	}
}